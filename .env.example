# Database
DATABASE_URL=sqlite:///./forwardo.db

# App
APP_SECRET_KEY=change-me-in-production
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000

# Optional: PostgreSQL (replace sqlite URL above)
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/forwardo
