# Poniedziałek 02:00 czasu polskiego (CEST = UTC+2). Zimą (CET) będzie 03:00.
name: Wysyłka Gmail poniedziałek

on:
  schedule:
    - cron: "0 0 * * 1"
  workflow_dispatch:
    inputs:
      allow_repo_fallback:
        description: "Gdy brak artifactu pipeline — użyj predykcje_2026.xlsx z repo"
        required: false
        default: true
        type: boolean

permissions:
  contents: read
  actions: read

jobs:
  send:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      PYTHONIOENCODING: utf-8
      GMAIL_USER: ${{ secrets.GMAIL_USER }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
      MAIL_TO: ${{ secrets.MAIL_TO }}
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Pobierz Excel
        run: |
          set -euo pipefail
          if [ -s predykcje_2026.xlsx ]; then
            echo "Używam predykcje_2026.xlsx z repozytorium (commit / CI)."
            exit 0
          fi
          ARGS=(
            --repo "${{ github.repository }}"
            --branch main
            --dest predykcje_2026.xlsx
          )
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.allow_repo_fallback }}" = "true" ]; then
            ARGS+=(--fallback predykcje_2026.xlsx)
          fi
          python scripts/fetch_predykcje_artifact.py "${ARGS[@]}" || {
            if [ -s predykcje_2026.xlsx ]; then
              echo "Artifact niedostępny — używam pliku z repo."
              exit 0
            fi
            exit 1
          }
          test -f predykcje_2026.xlsx

      - name: Wyślij mail
        run: python send_mail.py --plik predykcje_2026.xlsx
